A zip code has been submitted that does not reside in the database.  Determine if this zip code should be in the database, and take the appropriate steps.  Provide feedback to the requesting user when this task has been completed.  Here is the information that was input:

Site id:		<%= @site.id %>
Zip Code:		<%= @site.zip_code %>

By User (id):	<%= @site.user_id %>
